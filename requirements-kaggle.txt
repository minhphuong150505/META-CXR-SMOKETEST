# Keep Kaggle's preinstalled scientific/torch stack: torch, torchvision, CUDA,
# and also numpy / pandas / Pillow. Pinning numpy/pandas/Pillow to different
# versions forces downgrades that fight Kaggle's torch build and cause a
# numpy 1.x/2.x pickle mismatch between the training subprocess (which loads the
# pinned version from disk) and this kernel (which already holds Kaggle's numpy
# in memory via torch), breaking checkpoint save/load. Let Kaggle's versions
# stand; only install what Kaggle lacks or where the code needs a specific pin.
PyYAML==6.0.2
tqdm==4.67.1
transformers==4.53.2
tokenizers==0.21.4
huggingface-hub==0.33.4
omegaconf==2.3.0
iopath==0.1.10
timm==0.9.16
scikit-image==0.24.0
torchinfo==1.8.0
wandb==0.18.7
einops==0.8.0
fairscale==0.4.13
kaggle==2.2.3
kagglehub==1.0.2
nbformat>=5.10,<6
